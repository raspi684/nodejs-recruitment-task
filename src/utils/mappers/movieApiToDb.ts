import { KeyMapperType } from './mapper';

const mapper: KeyMapperType = [
  ['Title', 'title'],
  ['Released', 'released'],
  ['Genre', 'genre'],
  ['Director', 'director'],
];

export default mapper;
